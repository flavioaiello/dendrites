{
    "servers": {
        "dendrites": {
            "type": "stdio",
            "command": "~/.cargo/bin/dendrites",
            "args": ["serve", "--workspace", "${workspaceFolder}"]
        }
    }
}